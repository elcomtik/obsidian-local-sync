# Session Checkpoint — 2026-02-22

## What Was Done

1. **Project loaded** — Activated `obsidian-local-sync` in Serena, read all 5 source files.

2. **Full code analysis (`/sc:analyze`)** — 12 findings across bugs, architecture, quality, performance, security.
   See `issues-tracker.md` for full list.

3. **`/init` — Created `CLAUDE.md`** with build commands, architecture, data flow, key design details,
   and known issues tracker (updated throughout session).

4. **Mnemonic UX improvements (`src/main.ts`)**:
   - Reveal: inline read-only monospace input + toggle (Reveal/Hide), separate Copy button (clipboard)
   - Restore: textarea + Restore button on same `Setting` row with placeholder
   - Reset: two-click confirmation with 5-second timeout

5. **Bug fixes + performance (`/sc:improve`)**:
   - BUG-1: `getOrLoadFileState` — collapsed double vault read into one
   - BUG-2: `loadSettings` now saves settings immediately on first install to persist `deviceId`
   - BUG-3: `stop()` made `async` with `Promise.all` — awaits all closeDoc (flush + snapshot) before clearing state
   - PERF-1: `toBase64` — chunked `String.fromCharCode(...subarray)` (8192 B chunks)

6. **Version bumped to 0.0.3** — `manifest.json`, `package.json`, `CHANGELOG.md` updated.

7. **Issues written to `CLAUDE.md`** — Full tracker table with ✅/⚠ status, file:line references.

8. **`/sc:index` — Created `INDEX.md`** — Symbol reference for all 5 files, full method table for
   `YjsEvoluHistoryEngine`, Evolu table reference, config reference, data flow diagrams,
   ID convention table, dependency graph.

## Current State of Key Files

- `src/main.ts` — v0.0.3, mnemonic UX done, deviceId fix done
- `src/engine.ts` — v0.0.3, BUG-1/3/PERF-1 fixed
- `src/schema.ts` — unchanged
- `src/evoluClient.ts` — custom platform layer (replaced @evolu/web)
- `src/sqliteDriver.ts` — custom sql.js + Node.js fs persistence layer
- `CLAUDE.md` — architecture + full issues tracker
- `INDEX.md` — comprehensive symbol/data flow reference (NEW this session)
- `CHANGELOG.md` — 0.0.1 → 0.0.3

## Remaining Open Work (Priority Order)

1. ARCH-1: Skip own fileUpdate rows in poll (self-echo optimization)
2. ARCH-3: Defer snapshot write to end-of-batch instead of per-update
3. ARCH-2: Collapse N+1 poll queries into a single join
4. SEC-2: Relay URL validation (wss:/ws: scheme check before save)
5. QUAL-1: Type `evolu` properly in main.ts instead of `any`
